using UnityEngine;

// TODO(Step 5): real fields (stanceName, baseDamageMultiplier, postureDamageMultiplier,
// attackSpeedScalar, parryWindowDuration, icon) + [CreateAssetMenu].
public class StanceData : ScriptableObject
{
}
